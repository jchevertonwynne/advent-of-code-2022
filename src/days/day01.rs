use crate::days::Answers;
use crate::days::DayResult;

pub fn run() -> anyhow::Result<DayResult> {
    Ok(DayResult {
        part1: Some(Answers::Int(4)),
        part2: Some(Answers::Int(4)),
    })
}
